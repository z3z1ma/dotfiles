status is-interactive
or exit 0

# Disabled: starship prompt rendering is already fast once shell startup
# work is cached, and spawning child fish processes added more latency than it
# removed on this machine.
