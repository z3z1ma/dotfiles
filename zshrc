# Bootstrapping script for fish shell on macOS
# export XDG_CONFIG_HOME="${HOME}/.config" PATH="${PATH}:/usr/local/bin"

# if command -v devbox &> /dev/null
# then
#   eval "$(devbox global shellenv)"
# fi
#
# fish || echo "Run setup.sh in the dotfiles repo"
